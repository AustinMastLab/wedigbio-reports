<div class="{{ $containerClass ?? 'mb-6' }}">
    <div class="{{ $innerClass ?? 'mb-2 flex items-baseline gap-4' }}">
        <h1 class="{{ $titleClass ?? 'm-0 w-full text-center text-2xl font-bold text-slate-800' }}">{{ $title }}</h1>
    </div>
</div>

