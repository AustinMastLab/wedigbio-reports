<div class="card-dark text-center">
    <div id="{{ $valueId }}" class="text-2xl font-bold {{ $valueClass }}">{{ $value ?? '—' }}</div>
    <div class="mt-1 text-xs text-slate-500 dark:text-gray-400">{{ $label }}</div>
</div>

